# Continuation Protocol

**State:** `ACTIVE_NORMATIVE`  
**Policy:** `GIT_REPOSITORY_SSOT_V1`

## Session start

1. Verify repository `youtube-ai-factory`, branch `main`, exact HEAD, remotes and worktree.
2. Read `docs/README.md` in its required order.
3. Read only active documents for the current roadmap phase; do not load `docs/archive` by default.
4. Reconcile runtime/provider/spend evidence only when the authorized task needs it.
5. Name one bounded outcome, acceptance evidence, next action and protected scope.

## Session close

Update current state, active decision, issue and roadmap truth when materially changed. Run documentation and relevant source gates, create one bounded checkpoint, push the exact commit to both configured mirrors and verify a clean worktree.

## Recovery

Ordinary recovery uses either verified mirror of the same Git object graph. Archives, patches, Drive, Library and chats are discovery/recovery aids only and must be reconciled into Git before gaining authority.

## Protected handoff

- R21 is immutable visual FAIL/audio PASS evidence.
- R22 is design-only and undispatched.
- Phase 45 contracts/runtime and qualification precede R22.
- The private GitHub mirror is synchronized; every material session must re-prove exact three-ref equality before close.
- `youtube-ai-factory-v2` is prohibited as input.
- No auto-publish, hard-gate weakening or infinite repair loop.
